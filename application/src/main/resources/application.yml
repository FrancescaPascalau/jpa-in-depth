server:
  port: 8090

spring:
  datasource:
    url: jdbc:h2:mem:jpa
    username: user
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddlAuto: create-drop
  h2:
    console.enabled: true
